{#
  The fixed background — a near-silent dot texture (see .bg-orbs in input.css),
  included once in base.njk, behind all content (-z-10). No floating colour, no
  motion: a premium paper-white ground, not an instrument panel.
#}
<div class="bg-orbs" aria-hidden="true"></div>
